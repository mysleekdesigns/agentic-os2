# @agent-os/web

The Agent OS optional dashboard. Don't run this directly; it is started by `agent-os dashboard start` (Phase 15 CLI subcommand). Reads the same SQLite DB as the CLI; opens read-only.
