---
description: Sync Agent OS agents into the registry and mirror to .claude/agents
---

Run the Agent OS CLI command `agent-os agent sync` from the repository root. Report the summary of inserted/updated/unchanged registry rows and written/removed mirror files. Forward any additional flags (such as `--json`) verbatim.
