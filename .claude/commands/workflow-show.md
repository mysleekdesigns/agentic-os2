---
description: Show details of an Agent OS workflow run by id
---

Run the Agent OS CLI command `agent-os workflow show <run-id>` from the repository root, substituting `<run-id>` with the run id the user provided. Render the run summary, the per-step table, and any pending approvals to the user. Forward any additional flags (such as `--json`) verbatim.
