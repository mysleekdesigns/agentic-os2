---
description: Show details of an Agent OS approval request by id
---

Run the Agent OS CLI command `agent-os approvals show <id>` from the repository root, substituting `<id>` with the approval id the user provided. Render the approval row and its related event log to the user. Forward any additional flags (such as `--json`) verbatim.
