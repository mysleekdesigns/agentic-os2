---
description: List Agent OS providers with capability and api-key status
---

Run the Agent OS CLI command `agent-os provider list` from the repository root and show the user the rendered table. Forward `--json` verbatim if requested. Output reports each provider's `enabled` flag, api-key requirement / presence, capability matrix, and whether a factory has been registered for it.
