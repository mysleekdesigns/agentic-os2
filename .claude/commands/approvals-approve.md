---
description: Approve an Agent OS approval request
---

Run the Agent OS CLI command `agent-os approvals approve <id>` from the repository root, substituting `<id>` with the approval id the user provided. If the user supplies a note, pass it through as `--note "<text>"`. Confirm the approved id back to the user. Forward any additional flags verbatim.
