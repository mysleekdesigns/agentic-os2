---
description: Show details of an Agent OS agent by id
---

Run the Agent OS CLI command `agent-os agent show <id>` from the repository root, substituting `<id>` with the agent id the user provided. Show the rendered frontmatter and instruction body to the user. Forward any additional flags (such as `--json`) verbatim.
