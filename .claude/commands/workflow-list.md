---
description: List Agent OS workflows in this workspace
---

Run the Agent OS CLI command `agent-os workflow list --json` from the repository root, then summarise the workflows (id, version, description, path) for the user in a short table. If the user passes additional flags, forward them verbatim. Use this when the user asks to "list workflows", "show all workflows", or "what workflows are available".
