---
description: List Agent OS agents in this workspace
---

Run the Agent OS CLI command `agent-os agent list` from the repository root and show the user the rendered table. If the user passes a flag (such as `--json`), forward it verbatim.
